# 📖 คู่มือการติดตั้งและตั้งค่าระบบ Helpdesk เบื้องต้น
**(ฉบับสำหรับผู้เริ่มต้น ไม่เคยใช้งานมาก่อน)**

คู่มือฉบับนี้จัดทำขึ้นเพื่อให้คุณสามารถติดตั้งและเปิดใช้งานระบบ **Corporate Helpdesk & IT Support System** บนเครื่องคอมพิวเตอร์ของคุณได้อย่างง่ายดาย ทำตามทีละขั้นตอนได้เลยครับ!

---

## 📌 สารบัญ
1. [สิ่งที่ต้องเตรียมก่อนติดตั้ง](#1-สิ่งที่ต้องเตรียมก่อนติดตั้ง)
2. [ขั้นตอนการติดตั้งบน Windows](#2-ขั้นตอนการติดตั้งบน-windows)
3. [ขั้นตอนการติดตั้งบน Mac / Linux](#3-ขั้นตอนการติดตั้งบน-mac--linux)
4. [การเปิดใช้งานครั้งแรก (First Run)](#4-การเปิดใช้งานครั้งแรก-first-run)
5. [การตั้งค่าระบบเบื้องต้น (Admin Settings)](#5-การตั้งค่าระบบเบื้องต้น-admin-settings)
6. [การปิดระบบ](#6-การปิดระบบ)
7. [คำถามที่พบบ่อย (FAQ)](#7-คำถามที่พบบ่อย-faq)

---

## 1. สิ่งที่ต้องเตรียมก่อนติดตั้ง
เครื่องคอมพิวเตอร์ของคุณจำเป็นต้องมีโปรแกรมเหล่านี้ก่อน:

### 1.1 ติดตั้ง Python
*ระบบนี้ถูกพัฒนาด้วยภาษา Python ดังนั้นเครื่องของคุณต้องมี Python ติดตั้งอยู่*
1. เข้าไปที่เว็บไซต์: [https://www.python.org/downloads/](https://www.python.org/downloads/)
2. กดปุ่มดาวน์โหลด Python รุ่นล่าสุด (เช่น Python 3.11 หรือ 3.12)
3. เปิดไฟล์ที่ดาวน์โหลดมา **(สำคัญมาก: ในหน้าแรกของการติดตั้ง ให้ติ๊กถูกที่กล่อง "Add Python to PATH" หรือ "Add python.exe to PATH" ก่อนกด Install Now)**
4. รอจนติดตั้งเสร็จและกด Close

### 1.2 ดาวน์โหลดตัวระบบ (Source Code)
1. นำโฟลเดอร์ของโปรเจกต์ (ชื่อว่า `helpdesk_project`) มาวางไว้บนหน้า Desktop ของคุณ
2. ตรวจสอบให้แน่ใจว่าภายในโฟลเดอร์มีไฟล์ชื่อ `run.py`, `requirements.txt`, และโฟลเดอร์ `app`

---

## 2. ขั้นตอนการติดตั้งบน Windows

หากคุณใช้คอมพิวเตอร์ระบบปฏิบัติการ Windows ให้ทำตามขั้นตอนนี้:

1. เปิดโฟลเดอร์ `helpdesk_project` ขึ้นมา
2. คลิกที่แถบที่อยู่ด้านบนสุดของหน้าต่าง (Address bar) ที่เขียนว่าที่อยู่โฟลเดอร์ พิมพ์คำว่า `cmd` แล้วกด Enter
3. หน้าต่างสีดำ (Command Prompt) จะเด้งขึ้นมา
4. พิมพ์คำสั่งด้านล่างนี้ลงไป แล้วกด Enter (เพื่อสร้างสภาพแวดล้อมจำลองสำหรับรันระบบ):
   ```bash
   python -m venv venv
   ```
   *(รอประมาณ 10-20 วินาที หน้าจอจะไม่มีอะไรเกิดขึ้น ให้รอจนกว่าจะมีบรรทัดใหม่ขึ้นมา)*
5. พิมพ์คำสั่งด้านล่างนี้เพื่อเปิดใช้งานสภาพแวดล้อมจำลอง:
   ```bash
   .\venv\Scripts\activate
   ```
   *(สังเกตว่าจะมีคำว่า `(venv)` ขึ้นมาด้านหน้าบรรทัด)*
6. พิมพ์คำสั่งด้านล่างนี้เพื่อติดตั้งส่วนเสริมที่ระบบต้องใช้:
   ```bash
   pip install -r requirements.txt
   ```
   *(รอให้ระบบดาวน์โหลดไฟล์ต่างๆ จนเสร็จสิ้นและกลับมาบรรทัดใหม่)*

**🎉 การติดตั้งเสร็จสมบูรณ์!** ให้ทำต่อในหัวข้อ [การเปิดใช้งานครั้งแรก](#4-การเปิดใช้งานครั้งแรก-first-run)

---

## 3. ขั้นตอนการติดตั้งบน Mac / Linux

หากคุณใช้ Mac หรือ Linux ให้ทำตามขั้นตอนนี้:

1. เปิดแอปพลิเคชัน **Terminal**
2. พิมพ์คำสั่ง `cd` ตามด้วยเว้นวรรค 1 ครั้ง แล้วลากโฟลเดอร์ `helpdesk_project` มาใส่ในหน้าต่าง Terminal แล้วกด Enter
3. พิมพ์คำสั่งสร้างสภาพแวดล้อมจำลอง:
   ```bash
   python3 -m venv venv
   ```
4. พิมพ์คำสั่งเปิดใช้งาน:
   ```bash
   source venv/bin/activate
   ```
5. พิมพ์คำสั่งติดตั้งส่วนเสริม:
   ```bash
   pip install -r requirements.txt
   ```

**🎉 การติดตั้งเสร็จสมบูรณ์!** ให้ทำต่อในหัวข้อ [การเปิดใช้งานครั้งแรก](#4-การเปิดใช้งานครั้งแรก-first-run)

---

## 4. การเปิดใช้งานครั้งแรก (First Run)

เมื่อติดตั้งเสร็จแล้ว เรามาลองเปิดระบบกันเลย!
(ในหน้าต่างดำๆ หรือ Terminal ของคุณที่ยังมีคำว่า `(venv)` นำหน้า)

1. พิมพ์คำสั่งนี้เพื่อเปิดเซิร์ฟเวอร์:
   ```bash
   python run.py
   ```
2. หากถูกต้อง จะมีข้อความแสดงขึ้นมาว่า `* Running on http://127.0.0.1:5000`
3. อย่าปิดหน้าต่างสีดำนี้! ให้พับเก็บไว้ลงไป (Minimize)
4. เปิดเว็บเบราว์เซอร์ของคุณขึ้นมา (Google Chrome, Edge, Safari หรืออื่นๆ)
5. พิมพ์ที่ช่องค้นหาด้านบนสุด (Address bar) ว่า:
   ```text
   http://127.0.0.1:5000
   ```
6. กด Enter คุณจะพบกับหน้า Login ของระบบ Helpdesk!

### 🔑 บัญชีสำหรับการเข้าใช้งานครั้งแรก
ระบบได้สร้างบัญชีผู้ใช้งานตัวอย่างให้คุณแล้ว 5 บัญชี คุณสามารถใช้บัญชี Admin ในการเข้าสู่ระบบครั้งแรก:
- **ชื่อผู้ใช้ (Username):** `admin`
- **รหัสผ่าน (Password):** `1234`

---

## 5. การตั้งค่าระบบเบื้องต้น (Admin Settings)

หลังจากเข้าสู่ระบบด้วยบัญชี Admin แล้ว แนะนำให้ตั้งค่าระบบก่อนใช้งานจริง:

### 5.1 การจัดการผู้ใช้งาน (เพิ่มพนักงาน)
1. ดูที่แถบเมนูด้านซ้าย มองหาหมวด **"จัดการ"**
2. คลิกเมนู **"ผู้ใช้งาน"**
3. ที่หน้านี้ คุณสามารถเปลี่ยนระดับสิทธิ์ (Role) ของผู้ใช้งานในระบบได้ (เช่น เปลี่ยนเป็น Admin, Manager, Employee)
4. หากต้องการเพิ่มพนักงานใหม่ แนะนำให้พนักงานกดปุ่ม "สมัครสมาชิก" ที่หน้า Login แล้วรอให้ Admin มาปรับสิทธิ์ในหน้านี้

### 5.2 การจัดการหมวดหมู่งานซ่อม
1. ไปที่เมนู **"หมวดหมู่งาน"**
2. คุณสามารถกด **"สร้างหมวดหมู่ใหม่"** เพื่อกำหนดประเภทปัญหาของบริษัทคุณ (เช่น ซอฟต์แวร์, ฮาร์ดแวร์, ซ่อมบำรุงอาคาร)
3. สามารถใส่อีโมจิ (Emoji) เพื่อใช้เป็นไอคอนหมวดหมู่ให้สวยงามได้

### 5.3 การตั้งค่าการแจ้งเตือน (LINE / Email / Telegram)
1. ไปที่เมนู **"ตั้งค่า"**
2. หากบริษัทของคุณใช้ **LINE** เป็นหลัก คุณสามารถเปิดใช้งาน "แจ้งเตือนผ่าน LINE Notify" ได้
   - นำ Token ของ LINE Notify มากรอกในช่อง
3. หากกดบันทึกแล้ว เมื่อมีพนักงานแจ้งปัญหาซ่อมใหม่ หรือมีการอัปเดตสถานะงาน ระบบจะส่งข้อความเข้า LINE หรือช่องทางที่คุณตั้งค่าไว้ทันที!

### 5.4 การสร้างบทความฐานความรู้ (Knowledge Base)
1. ไปที่เมนู **"จัดการฐานความรู้"**
2. กดปุ่ม **"สร้างบทความใหม่"**
3. เขียนวิธีแก้ปัญหาเบื้องต้นต่างๆ เพื่อให้พนักงานสามารถเข้ามาอ่านและแก้ปัญหาด้วยตนเองก่อนที่จะกดแจ้งปัญหา IT ได้

### 5.5 การจัดการอุปกรณ์ในแผนก (Equipment Management)
1. ไปที่เมนู **"อุปกรณ์ในแผนก"**
2. คุณสามารถเพิ่มรายการอุปกรณ์ที่แผนกดูแลอยู่ (สว่าน, บันได, เครื่องวัดไฟ ฯลฯ)
3. กำหนดประเภทเป็น **"อยู่กับที่"** หรือ **"เคลื่อนที่"** (สำหรับอุปกรณ์ที่ต้องติดรถไปหน้างาน)
4. ระบบจะบังคับให้ถ่ายรูปทุกครั้งที่มีการ ยืม หรือ คืน เพื่อเป็นหลักฐานความรับผิดชอบครับ

---

## 6. การปิดระบบ

เมื่อคุณต้องการปิดระบบ หรือเลิกใช้งาน:
1. กลับไปที่หน้าต่างสีดำ (Command Prompt / Terminal)
2. กดปุ่ม **`Ctrl + C`** บนแป้นพิมพ์ค้างไว้พร้อมกัน (อาจจะต้องกด 2-3 ครั้ง) เพื่อสั่งหยุดเซิร์ฟเวอร์
3. หากต้องการปิดระบบจำลอง ให้พิมพ์คำสั่ง:
   ```bash
   deactivate
   ```
4. สามารถกดกากบาทเพื่อปิดหน้าต่างสีดำได้เลย

*(หากต้องการใช้งานครั้งหน้า ให้ทำซ้ำขั้นตอน เปิด cmd -> `.\venv\Scripts\activate` -> `python run.py` ได้เลยครับ ไม่ต้องติดตั้งใหม่)*

---

## 7. คำถามที่พบบ่อย (FAQ)

**Q: หน้าเว็บขึ้น Error ว่า `Site can't be reached`?**
A: เซิร์ฟเวอร์น่าจะถูกปิดไปแล้ว ตรวจสอบว่าหน้าต่างสีดำ (Command Prompt) ยังเปิดอยู่และกำลังรันคำสั่ง `python run.py` อยู่หรือไม่

**Q: ลืมรหัสผ่าน Admin ทำอย่างไร?**
A: เนื่องจากเป็นระบบความปลอดภัยสูง หากลืมรหัสผ่านของระบบ จะต้องเข้าไปแก้ในฐานข้อมูลโดยตรง หรือลบไฟล์ `instance/helpdesk.db` เพื่อให้ระบบสร้างใหม่ทั้งหมด (แต่ข้อมูลเก่าจะหายหมด) แนะนำให้จดรหัสผ่านไว้ให้ดีครับ

**Q: อยากให้เพื่อนในออฟฟิศเข้าใช้งานได้ด้วย ต้องทำอย่างไร?**
A: เบื้องต้น `127.0.0.1` คือการเข้าใช้เฉพาะเครื่องของคุณเอง หากต้องการให้คนอื่นเข้าได้ คอมพิวเตอร์ของคุณต้องเชื่อมต่อ Wi-Fi/LAN วงเดียวกันกับเพื่อน จากนั้นหาที่อยู่ IP ของเครื่องคุณ (เช่น `192.168.1.50`) และให้เพื่อนพิมพ์ในเบราว์เซอร์ว่า `http://192.168.1.50:5000` (อาจจะต้องตั้งค่า Firewall ในเครื่องของคุณด้วย) หรือแนะนำให้นำระบบขึ้น Cloud server เพื่อการใช้งานจริงครับ

---
**จบการตั้งค่าขอให้สนุกกับการใช้งานระบบครับ! 🎉**
